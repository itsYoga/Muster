# Copy to .env (gitignored) and fill in one of these. Loaded automatically
# when backend == "llm"; real environment variables take precedence.
GEMINI_API_KEY=
# ANTHROPIC_API_KEY=
# MUSTER_LLM_PROVIDER=gemini
# MUSTER_LLM_MODEL=gemini-flash-latest
